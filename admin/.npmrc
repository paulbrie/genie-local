# @xterm/addon-canvas@0.7 still declares a peer of @xterm/xterm@^5, but it runs
# fine against xterm 6 (same combo genie ships). Accept it so `npm install`
# (including deploy/install.sh) doesn't ERESOLVE on the stale peer range.
legacy-peer-deps=true
