# GENERATED from the admin DB (apps.port). Do not edit by hand;
# it is overwritten whenever a port is changed in the admin UI.
# The prefix is PRESERVED (no strip): each app must set its Next basePath
# to the mount path shown in the comment.

# hmetal/metal-creativ — app basePath must be '/projects/hmetal'
location /projects/hmetal { proxy_pass http://127.0.0.1:4100; }

# research/(root) — app basePath must be '/projects/research'
location /projects/research { proxy_pass http://127.0.0.1:3020; }

# roa/server-admin — app basePath must be '/projects/roa/server-admin'
location /projects/roa/server-admin { proxy_pass http://127.0.0.1:4112; }

# roa/server-app — app basePath must be '/projects/roa/server-app'
location /projects/roa/server-app { proxy_pass http://127.0.0.1:4111; }
